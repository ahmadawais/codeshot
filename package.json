{
  "name": "codeshot",
  "displayName": "codeshot",
  "description": "Your codeshot",
  "version": "0.0.1",
  "publisher": "ahmadawais",
  "engines": {
    "vscode": "^1.20.0"
  },
  "categories": ["Other"],
  "activationEvents": ["onCommand:codeshot.sayHello"],
  "main": "./out/extension",
  "contributes": {
    "commands": [
      {
        "command": "codeshot.sayHello",
        "title": "📸  CodeShot"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "postinstall": "node ./node_modules/vscode/bin/install",
    "test": "npm run compile && node ./node_modules/vscode/bin/test"
  },
  "devDependencies": {
    "typescript": "^2.6.1",
    "vscode": "^1.1.6",
    "tslint": "^5.8.0",
    "@types/node": "^9.4.6",
    "@types/mocha": "^2.2.42"
  }
}
